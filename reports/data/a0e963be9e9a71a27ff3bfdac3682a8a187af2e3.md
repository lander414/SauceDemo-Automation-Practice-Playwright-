# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: Functional Testing\Checkout\Verify_Checkout_Invalid_PostalCode.spec.ts >> Verify Checkout Invalid Postal Code
- Location: tests\Functional Testing\Checkout\Verify_Checkout_Invalid_PostalCode.spec.ts:5:5

# Error details

```
Error: locator.fill: Target page, context or browser has been closed
Call log:
  - waiting for getByPlaceholder('Username')

```

# Test source

```ts
  1  | import type { Locator, Page } from '@playwright/test';
  2  | import { loginLocators } from '../locators/loginLocators';
  3  | 
  4  | export class LoginPage {
  5  |   readonly username: Locator;
  6  |   readonly password: Locator;
  7  |   readonly loginButton: Locator;
  8  | 
  9  |   constructor(private readonly page: Page) {
  10 |     this.username = page.getByPlaceholder(loginLocators.username);
  11 |     this.password = page.getByPlaceholder(loginLocators.password);
  12 |     this.loginButton = page.getByRole('button', { name: loginLocators.loginButton });
  13 |   }
  14 | 
  15 |   async open(): Promise<void> {
  16 |     await this.page.goto('/');
  17 |   }
  18 | 
  19 |   async login(username: string, password: string): Promise<void> {
> 20 |     await this.username.fill(username);
     |                         ^ Error: locator.fill: Target page, context or browser has been closed
  21 |     await this.password.fill(password);
  22 |     await this.loginButton.click();
  23 |   }
  24 | }
```